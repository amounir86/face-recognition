function [tFaces, tLabels] = chooseTrainingData(allFaces, allLabels)
% ChooseTrainingData, this function chooses some data to be the training
% data of the system.
%
% INPUT: allFaces, a variable containing all of the faces that exist in the
%                  sytem.
%        allLabels, the labels of the faces of the system.
%
% OUTPUT: tFaces, the training faces for the classifier.
%         tLabels, the labels of the training faces.

    % Prepare the eigen faces array
    
    
    % Display the plot to the user
    
    
    % Read the value of the user requires for the eigen faces
    
    
    % Return the top user selected value eigen faces


end

